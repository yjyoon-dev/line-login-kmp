## What this changes

<!-- One or two sentences. Link the issue it resolves, if there is one. -->

## Why

<!-- What was wrong, or what became possible. -->

## Checklist

- [ ] `./gradlew spotlessApply` is clean
- [ ] `./gradlew :lineloginkmp:allTests` passes
- [ ] `./gradlew :lineloginkmp:checkKotlinAbi` passes, or the API dump was updated on purpose
- [ ] If `iosMain` changed: the iOS sample still builds
- [ ] If behaviour changed: verified on a real device, and said so below

## Verified on

<!-- e.g. "Pixel 7, Android 15, with and without the LINE app installed" — or "not device-tested". -->
